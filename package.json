{
  "name": "cron-checker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "test": "npm run build && nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "npm run build && sls deploy"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.13.1",
    "body-parser": "^1.19.0",
    "bootstrap": "^4.6.0",
    "bootstrap-vue": "^2.21.2",
    "core-js": "^3.9.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.7",
    "nuxt": "^2.15.3",
    "serverless-http": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
